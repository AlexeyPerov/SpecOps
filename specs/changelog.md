# Changelog

## 2026-06-07

- **Chat-http send + workspace switch freeze.** Fixed chat-http sends going to a draft agent while the UI still followed a stale workspace agent tab (`$effect` tab sync now skips chat-http). `ensureChatHttpAgentTab` always focuses the active chat agent instead of bailing when any agent tab exists. `switchThreadConnection` now creates a thread for draft agents so composer connection fallback cannot spin forever.
- **Chat-http empty-state and model selection.** Fixed blank model dropdown on chat open by resolving composer models from the active HTTP connection catalog (not global defaults). Model/connection/provider switches now ensure a draft agent exists; `loadWorkspaceAgents` preserves in-memory session drafts across async disk loads; chat-http init waits for agent index load before creating tabs. Composer silently initializes default model metadata; configuration switch errors no longer show misleading “Tap Retry” hints.
- **Chat diagnostics and connection model validation.** Added structured chat diagnostics logging (provider/connection/model switches, send lifecycle, HTTP request/response/stream events) to the app console and Tauri log via `chatDiagnostics.ts`. Fixed HTTP model validation to use per-connection catalogs for send blocking, active model resolution, provider/model switching, and composer model lists instead of the legacy global `providerModelCatalogs.http` slot.
- **Settings → Providers layout.** Added spacing and a divider above **Selected connection**, and moved **Remove** onto each provider row in the connection list with a confirmation dialog before deletion.
- **Settings dialog as floating window.** Reworked `SettingsDialog` from a centered modal with backdrop dismiss into a draggable, resizable floating panel: no dimmed overlay, clicks pass through to the app behind it, position and size persist for the session, and Escape / the close button still dismiss it. Added `settingsDialogGeometry` helpers for viewport clamping.

## 2026-06-06

- **2026-06-07 09:58 (UTC+3) — M4 task 7 completed (tests, validation, docs, milestone closeout).** Added focused resolver/configuration unit coverage for HTTP multi-connection helpers (`resolveHttpConnection`, configured-connection filtering, and legacy key fallback), verified full suite with `npm test` and `npm run check`, and marked phase-2 M4 execution/task checklists complete in `execution-plan-m4-multiple-providers.md` and `phase-2.md`.
- **2026-06-07 09:32 (UTC+3) — M4 tasks 5-6 implemented (composer connection picker + rail/restore gating).** Reworked chat composer provider selection into a connection-first flow (configured HTTP connections by label + scoped Debug Provider), wired per-thread HTTP `connectionId` switching with per-connection model lists and stale-connection fallback, and updated blocked copy to reference Providers. Updated chat-http rail visibility and active-context restore gating to require at least one configured connection with a resolvable per-connection default model (or debug-chat enabled), with validation tests adjusted for multi-connection behavior.
- **2026-06-07 09:09 (UTC+3) — M4 tasks 3-4 implemented (connection-aware send path + Providers list UI).** Updated HTTP provider runtime/capability flow to resolve settings by thread `connectionId`, persisted default connection on HTTP thread/provider switches, and added explicit thread connection switching with per-connection model fallback. Reworked Settings → Providers from a singleton form into a multi-connection list editor (add/select/edit/remove/default, per-connection API key + model catalog), preserving deep-link behavior and access preflight refresh semantics.
- **2026-06-06 23:39 (UTC+3) — M4 tasks 1-2 implemented (multi-connection schema + resolver/secrets).** Added HTTP connection schema foundations (`httpConnections`, `defaultConnectionId`, `connectionId` on thread metadata), legacy load-time normalization for singleton `http`, connection-level resolver helpers and configured-list checks, and connection-keyed secrets APIs with legacy `keys.http` read mapping to `default`. Wired app bootstrap/settings/runtime to load/store per-connection keys while preserving legacy compatibility paths, and updated affected provider, rail-gating, settings, and validation tests.
- **Phase 2 M4 execution plan — multiple HTTP providers.** Added `specs/ops/phase-2/execution-plan-m4-multiple-providers.md`: named OpenAI-compatible connections in Settings → Providers, per-connection secrets and model catalogs, composer connection → model picker, shared pool for Chats and workspace HTTP chat. Pulls phase-7 tier 2A only; P2-8 task id; updated `phase-2.md` execution chain and M4 exit criteria.
- **Chat-http provider and terminology fixes.** Chats UI now derives `chatContextKind` from the active app context (not only chat store scope), so the provider dropdown shows **Debug Provider** (not workspace Debug Agent) and hides **HTTP** until the connection is configured in both Chats and Workspaces. Switching chat scope normalizes in-memory thread providers (`debug-workspace` ↔ `debug-chat`). Settings: **Connections** renamed to **Providers**; debug settings tabs unified to **Debug Provider**. Chat-http uses “New chat”, “Message chat”, and auto-creates a draft chat on send when none is active.
- **Split Debug providers for Chats vs Workspaces.** Replaced the single `debug` provider with scoped ids `debug-chat` (Debug AI, Chats context, ask-only) and `debug-workspace` (Debug Agent, workspace agents, ask+review). Settings now store independent `providerSettings.debugChat` and `providerSettings.debugWorkspace` blocks (shared simulation shape). Settings sidebar: **Chats** → Connections + Debug AI; **Workspaces** → Debug Agent. Chat rail appears when HTTP is configured **or** Debug AI is enabled. Legacy persisted `provider: "debug"` normalizes to the scoped id by chat scope on load.
- **Settings sidebar categories.** Settings dialog sidebar now groups options into top-level **Editor** and **Shortcuts** tabs, with **Chats** and **Workspaces** section labels above **Connections** and **Debug AI** respectively. Section labels use smaller indent, bold 13px type; all selectable tabs share a larger uniform indent so headings are visually distinct from options.
- **Workspace rail reorder.** Workspace icons in the activity rail can be reordered by drag-and-drop (when two or more workspaces are open) or via right-click **Move Up** / **Move Down**. Order is persisted per window in the session snapshot. Active context is unchanged on reorder; new workspaces still append at the end. Added hidden `workspace.reorder` command for automation (`fromIndex` / `toIndex` payload).
